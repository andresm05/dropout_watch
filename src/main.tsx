import React from "react";
import ReactDOM from "react-dom/client";
import { DropoutWatch } from "./DropoutWatch.tsx";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <DropoutWatch />
  </React.StrictMode>
);
